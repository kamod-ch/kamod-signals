{
  "name": "@kamod-ch/signals-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "prepublishOnly": "node -e \"console.error('Docs are private and must not be published. Publish only packages/core.'); process.exit(1)\"",
    "dev": "preactpress dev",
    "check": "preactpress check",
    "build": "preactpress build",
    "preview": "preactpress preview"
  },
  "dependencies": {
    "@kamod-ch/signals": "workspace:*"
  },
  "devDependencies": {
    "@kamod-ch/preactpress": "^1.0.0",
    "typescript": "^6.0.3"
  }
}
