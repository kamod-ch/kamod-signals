# @kamod-ch/signals docs

```bash
pnpm install
pnpm docs:dev
```

Build for GitHub Pages:

```bash
pnpm docs:build
```

Deployment runs automatically via GitHub Actions from `.github/workflows/gh-pages.yml` on pushes to `main`.
